fastapi>=0.115
uvicorn[standard]>=0.30
gunicorn>=22.0
sqlalchemy>=2.0
pydantic>=2.9
pyjwt>=2.9
requests>=2.31

# Production (Turso) — verified working, uncomment before deploying:
sqlalchemy-libsql
libsql-client

# Production rate limiting shared across workers — uncomment once you have
# a Redis instance (Render add-on, Upstash free tier, etc.) and set REDIS_URL:
# redis
